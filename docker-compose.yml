# Local development only. Production builds straight from the Dockerfile's
# `production` target — see the README for that invocation.
services:
  db:
    image: postgres:16-alpine
    environment:
      POSTGRES_DB: ${DB_DATABASE}
      POSTGRES_USER: ${DB_USERNAME}
      POSTGRES_PASSWORD: ${DB_PASSWORD}
    volumes:
      - esavods-db-data:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U ${DB_USERNAME} -d ${DB_DATABASE}"]
      interval: 5s
      timeout: 5s
      retries: 10
    ports:
      - "5432:5432"

  app:
    build:
      context: .
      target: dev
    volumes:
      - .:/var/www
    env_file:
      - .env
    depends_on:
      db:
        condition: service_healthy

  nginx:
    image: nginx:1.27-alpine
    depends_on:
      - app
    volumes:
      - .:/var/www:ro
      - ./docker-compose/nginx/dev:/etc/nginx/conf.d:ro
    ports:
      - "8080:80"

volumes:
  esavods-db-data:

; The image default of 128M is shared memory this box cannot spare; 48M was
; measured as sufficient for an app this size. validate_timestamps stays at
; its default (on) deliberately — the win from disabling it is small and it
; would make dev behave differently from production.
opcache.memory_consumption=48
